[pytest]
# Repo root on sys.path so `common`, `etl_pipeline`, ... import in tests.
pythonpath = .
addopts = -q
markers =
    spark: tests that spin up a local SparkSession (slower, needs Java)
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
